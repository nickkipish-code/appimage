{
  "mcpServers": {
    "n8n": {
      "command": "node",
      "args": ["mcp-server.js"],
      "env": {
        "N8N_API_URL": "${N8N_API_URL:-http://localhost:5678}",
        "N8N_API_KEY": "${N8N_API_KEY}"
      }
    }
  }
}
